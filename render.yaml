services:
  - type: web
    name: sentinel-ews-backend
    runtime: node
    plan: free
    buildCommand: cd backend && npm install
    startCommand: cd backend && node src/index.js
    envVars:
      - key: LLM_API_TOKEN
        sync: false
